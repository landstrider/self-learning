#include <stdio.h>

int main() 
{
//  char ch='\0';
//  char* caseIs[]; 
//  scanf("%s\n", &ch);
//  if(ch >= 'a' && ch <= 'z') caseIs = malloc(strlen("lower case"));
//  if(ch >= 'A' && ch <= 'Z') caseIs = "Upper case";
//  if(ch >= '0' && ch <= '9') caseIs = "digit";
//  printf("case of %s is\n", ch);
  c>=’a’&& c<=’z’
}
