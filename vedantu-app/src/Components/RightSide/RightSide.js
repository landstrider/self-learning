import React from 'react';
import Complete from './Components/AutoCompleteInput';
const RightSide = (props) => (
	<div> 
	  <Complete userrepodetails = {props.userrepos} />

	</div>
);

export default RightSide;
